
📝 [[#📝 Домашнее задание 3|Посмотреть Дз_3]]

---
# 🫡К третьей лекции вы должны быть:

- Зарегистрированы на сайте acmp.ru [Ссылка](https://acmp.ru/)
- Зарегистрированы на сайте projecteuler.net [Ссылка](https://projecteuler.net/archives)
- Зарегистрированы на сайте CodeForces [Ссылка](https://codeforces.com/)
- Зарегистрированы на сайте LeetCode [Ссылка](https://leetcode.com/) 

<p align="justify">Вначале 3 лекции выделим пару минут, на те темы, которые вы поняли хуже всего и проведем контрольную работу на 25 минут. </p>

![[Pasted image 20250120145431.png]]

---
# 🧐 Более подробно о переменных в Python 

<p align="justify"><font color="#7f7f7f">Напоминаю, что в Python переменных нет, просто так удобно объяснять информацию.</font></p>

Итак, в качестве переменных в Python могут выступать:

- Любые латинские буквы, 
- Цифры 
- Символ нижнего подчеркивания

1 - В именах нельзя использовать пробел.

<p align="justify">2 - Название должно начинаться с буквы или символа подчёркивания, но не с цифры.</p>

<p align="justify">3 - НЕ рекомендуется использовать буквы и слова других алфавитов (например, кириллицу)</p>

```python
# Так можно
a = 10 
_a = 80
__a = 3
а1 = 7
a_b = 10

# Вот так нельзя
1a = 10 
a b = 10
картошка = 20
```

<br>

4 - Имена переменных чувствительны к регистру, например:

```python 
a = 10 
A = 10
# Это разные переменные а и А
```

<br>

5 - Нельзя использовать зарезервированные (ключевые) слова:

```python
# ['False', 'None', 'True', 'and', 'as', 'assert', 'async', 'await', # 'break', 'class', 'continue', 'def', 'del', 'elif', 'else', 'except', # 'finally', 'for', 'from', 'global', 'if', 'import', 'in', 'is', 'lambda', # 'nonlocal', 'not', 'or', 'pass', 'raise', 'return', 'try', 'while', 'with', 'yield']
```

<p align="justify">Запоминать их не стоит, вы со временем все их узнаете, просто пробегитесь один раз глазами. </p>

---
# Типы присваивания

Позиционное

```python
x = 10
```

Множественное 

```python
a = b = c = 10
```

Каскадное

```python 
a, b, c = 1, 2, 3
```

<br>

<p align="justify">Эти названия запоминать не обязательно, главное знать, что такие присваивания в принципе есть. </p>

<br>

<p align="justify">А как поменять значения переменных местами? Для этого понадобится еще одна временная (temporary) переменная. </p>

```python
a = 10
b = 20
temp = a  # сохраняем значение а во временной переменной
a = b     # теперь а ссылается на тот же объект что и b
b = temp  # меняем ссылку b

print(a)
print(b)
```

<p align="justify">Этот пример становится хорошо понятным когда вы представляете что-то физическое. Например, у вас есть 2 стакана в одном вода, в другом молоко. Вам нужно перелить жидкости местами. Согласитесь что без 3 стакана не обойтись, так и здесь.</p>

![[0D_MI78RSqud6ZbmswOfQw.png]]

Но можно это сделать и через 2 переменные
```python
a = 10
b = 20

temp_1 = a
temp_2 = b

a = temp_2
b = temp_1
```

<p align="justify">Но если взглянуть внимательно на эту запись, что у нас получилось? Давайте запишем в строчку чтобы это выглядело более наглядно</p>

```python
a -> temp_2 = b 
     a = temp_2

b -> temp_1 = a 
     b = temp_1

# то есть на самом деле мы можем записать 
a, b = b, a
```

<p align="justify">Некоторые считают, что эта запись <code>a, b = b, a</code> осуществляется без дополнительных переменных, однако они ошибаются, так как тут задействованы аж 2 дополнительные переменные. </p>

<p align="justify">Чтобы это легко запомнить, опять приведем пример из жизни. Представьте вы готовите блины на 2х сковородках. Не знаю почему вам на одной не готовиться, видать уж очень вы любите блинчики, ну да ладно. Вот вы готовите, и пришло время их переворачивать. Вы подбрасываете блины, и перекрещиваете руки на крест. То есть первый блинчик падает на вторую сковороду, а второй на первую. </p>

Вот абсолютно тоже самое происходит и в данном случае с переменными. 

![[pQWGGLNbQte7sXKBNLK_0w.png]]

---
# 🤝 PEP 8 - Этика написания кода

<p align="justify"><font color="#00b050">PEP 8</font> — это руководство по стилю написания кода на Python, которое помогает делать код читаемым, понятным и единообразным. Это как свод правил хорошего тона для программистов, пишущих на Python.</p>

<p align="justify">Одна из целей курса - это научить вас грамотно и красиво оформлять программный код, чтобы он был понятным ни только вам, но и другим людям. Если вы не хотите чтобы вас за спиной называли 💩говнокодером, то первое с чего стоит начать это соблюдение правил из PEP 8. </p>
<br>
<p align="justify">В Pycharm горячие клавиши для оформления кода в PEP 8 это <font color="#00b050">Ctrl + Alt + L</font> </p>

<p align="justify">Сейчас вам не нужно углубляться в детали оформления кода по PEP 8, я буду объяснять это на уроках. Важно лишь знать, что такое оформление кода есть и если я скажу: "Это оформлено по PEP 8", вам должно быть ясно, что речь идёт о стандартном подходе оформления. </p>

![[12.png]]

---
# ✍️ STRING (STR)

<p align="justify"><font color="#00b050">str </font>– это тип данных, который используется для хранения текстовой информации. Строки позволяют работать с символами, словами, предложениями и любым текстом. Давайте разберем как их можно использовать:</p>

<p align="justify">Строчку можно создать обернув текст в одинарные или двойные кавычки или использовав ключевое слово str() </p>

```python
a = str()               # через ключевое слово str
x = 'Hi'                # через одинарные ковычки
y = "My name is Anna"   # через двойные ковычки

print(type(a))
print(type(x))
print(type(y))
```

<p align="justify">Принципиальной разницы тут нет. Однако хорошим тоном при написании программы считается использование какого-то одного типа кавычек. </p>

<br>

<p align="justify">Подобная типичная форма записи называется <font color="#00b050">литералом.</font> Например мы уже знаем, что если мы запишем числа 1, 11, 2919, 19519, то это int, если мы запишем 1.51, 2.31, 0.5, 0.0125 то это float, а если 'hello', "Привет" - то это str. </p>

<p align="justify">Для многострочного текста используются тройные кавычки. Напишем пример:</p>

```python
my_name = ''' Hi
My name is Annna'''

print(my_name) 
```

После исполнения данного кода на экране вы увидите надпись 

<center><code>Hi\nMy name is Anna</code> </center>

<p align="justify">Что это за загадочное <code>\n</code> ?  Заметьте что когда мы текст, слово <code>Hi</code> мы написали на одной строчке, а  <code>My name is Anna</code> на другой. Символ <code>\n</code> - как раз и означает перевод каретки на новую строчку. </p>

`\n` = Переход на новую строчку

<p align="justify"><font color="#00b050">Кареткой</font> - называется мигающая вертикальная палочка, которая показывает вам где вы сейчас будете печать текст. </p>

---
# print()

Давайте по подробнее рассмотрим функцию print() для этого запишем:

```python
a = 10
b = 20
print(a, b)
```

<p align="justify">На экране мы увидим числа 10 и 20 записанные через пробел. Хм.... А откуда там взялся пробел? </p>

<p align="justify">Оказывается у функций есть скрытые параметры, которые используют какие-то значения по умолчанию. Эти параметры называются <font color="#00b050">именованными</font>. </p>

<p align="justify">Рассмотрим 2 именованных параметра, которые скрываются в print() - это <font color="#00b050">end</font> и <font color="#00b050">sep</font> Начнем с sep. </p>

---
# sep

<p align="justify"><font color="#00b050">sep</font> = separate с англ. разделить, отсепарируй. Он отвечает за тип разделения между параметрами в print(). По умолчанию в нем стоит пробел.</p>


```python
print(a, b, sep=" ")
```

<br>

Давайте попробуем заменить разделитель на какой-нибудь другой.

```python
print(a, b, sep="???") # на экране увидем 10???20
```

<br>

или вот так вот:

```python
print(a, b, sep=123) 

# на экране увидем ошибку
TypeError: sep must be None or a string, not int
```

А вот и наша первая ошибка.

<p align="justify"><font color="#ff0000">Ошибки</font> - это часть программирования, и их нужно правильно интерпретировать. Чтобы хорошо понимать ошибки, мало разбираться в коде, нужно хотя бы знать базовые слова на английском. Да и в принципе английский и программирование идут рука об руку.  </p>

<p align="justify">В данной ошибке нам говориться, что в качестве <code>sep</code> может выступать или <code>None</code> или <code>str</code> но не тип <code>int</code></p>

Что такое `None` ?

---
# None type

![[криминальное.gif]]

<p align="justify"><font color="#00b050">None</font> — это специальное значение, которое обозначает отсутствие значения или пустоту. Это объект специального типа <font color="#00b050">NoneType</font>, и в языке Python он используется для представления «ничего», «неопределенности» или «отсутствия данных».</p>

```python
print(type(None))  # <class 'NoneType'>
```

<br>

<p align="justify"><code>None</code> — это единственный экземпляр типа <code>NoneType</code>. То есть у нас есть один тип NoneType и там только один объект None, обозначающий ничего. </p>

<br>

<p align="justify">Хотя <code>None</code> означает «ничего», это не то же самое, что числа или строки с пустым значением. </p>

None не тоже самое что 0 
None не тоже самое что пустая строка "" 

Они не равны потому что это разные типы, 
- type(0) = int, 
- type("") = str, 
- type(None) = NoneType

<br>

<p align="justify">Возвращаемся к <code>sep</code>. Получается в качестве sep может выступать только строки или None. Давайте проверим и убедимся в этом:</p>

```python
print(a, b, sep="123") # на экране увидем 1012320
print(a,b, sep=None) # на экране увидем 10 20
```

---
# end

<p align="justify"><font color="#00b050">end</font> = end line с англ. конец строки. По умолчанию в нем стоит <code>\n</code> то есть как мы уже знаем - это перевод каретки на новую строчку. Давайте в этом убедимся </p>

```python
a = 10
b = 20
print(a)
print(b)

# на экране увидем 
10
20
```

<br>

Попробуем изменить этот именованный параметр:

```python
a = 10
b = 20
print(a, end="")
print(b)

# на экране увидем 
10 20
```

<br>

<p align="justify">Отлично! Теперь данные выводятся в строчку. Но обратите внимание, что если мы захотим вывести следующее число, то оно опять окажется на новой строке. </p>

```python
a = 10
b = 20
c = 30
print(a, end="")
print(b)
print(c)

# на экране увидем 
10 20
30
```

<p align="justify">Так как мы указали, что не нужно переходить на новую строчку только в первом print() а во втором и третьем нет. </p>

<p align="justify">Как и в sep в качестве значений могут выступать только строки str и None. </p>

---
Итог, по умолчанию print работает вот так:  

```python
print(object_1, object_2, object_3,..., end="\n", sep=" ")
```

Такая запись называется - <font color="#00b050">запись в общем виде</font>

- В ней object1, object2, object_3,..., - это позиционные аргументы;
- end и sep - это именованные параметры</font.

---
## INPUT()

<p align="justify">Теперь научимся вводить данные с клавиатуры и в этом нам поможет функция input()</p>

<br>

<p align="justify">Давайте попробуем ввести что-нибудь и посмотрим что в итоге получиться:</p>

```python
a = input()  # Например введем свое имя

print(a)     #  Arsen
print(type(a)) # <class 'str'> 
```

<br>

Видим, что тип тех данных, которые мы ввели `str`. Давайте попробуем ввести числа:

```python
a = input()  # Например введем свое 123

print(a)     #  123
print(type(a)) # <class 'str'> 
```

Тип все равно остался `str` в чем же дело?  Оказывается input() после ввода превращает все наши данные в тип `str` будь то это числа, строки, специальные знаки - все по итогу будет преобразовано в строчный тип.

<br>

Для того чтобы преобразовать тип str в int нужно записать вот так

```python
a = int(input())  # Например введем свое 123

print(a)     #  123
print(type(a)) # <class 'str'> 
```

<p align="justify">Почему именно так, а например не так <code>print(int(a))</code> или <code>print(type(int(a)))</code> мы узнаем немного позже. </p>

<br>

Давайте попробуем записать значения в 2 переменные:

```python
a = input()
b = input()

print(a,b)
```

---
# Арифметические операции над строками

<p align="justify">В Python строки можно складывать - это называется <font color="#00b050">конкатенацией строк</font>. В результате получается как бы склеенный текст:</p>

```python
a = input() # abc
b = input() # 123

print(a + b) # abc123
```

<p align="justify">! Еще раз обращаю ваше внимание, что в данном случае 123 является строкой а не числом. </p>

<br>

Если хотя бы одно слагаемое будет не строкой, произойдет ошибка. 

```python
a = input() # abc
b = int(input()) # 123

print(a + b) # TypeEror
```

<p align="justify">То есть складываем числа с числами, строки со строками и никак иначе. </p>

<br>

<p align="justify">Далее, строки можно клонировать, делается это путем умножения на целое число:</p>

```python
a = input() # abc

print(a * 3) # abcabcabc
```

Причем умножать можно<font color="#00b050"> только на целое число</font> 

```python
a = "abc"
b = 2.5

print(a * b)  # Ошибка TypeError
```

<br>

<p align="justify">Со строками нельзя выполнять такие операции, как вычитание (`-`), деление (`/`), остаток от деления (`%`) и т. д. Любая попытка использовать недопустимую операцию приведет к ошибке:</p>

---
# 🧮 Первые задачи

И последнее что вам придется сегодня запомнить это вот такая запись 

```python
a, b = map(int, input().split())
```

<p align="justify">Ее придется выучить наизусть, так как она очень часто используется в задачах, а объяснять как она работает еще проблематично. </p>

<br>

<p align="justify">Когда ее стоит использовать? Например, когда вас в задачах просят считать числа записанные в одну строчку через пробел. </p>

```python
Даны 3 переменные ->  10 11 12

a, b, c = map(int, input().split())

print(a) # 10
print(b) # 11
print(c) # 12
```

Давайте сразу для закрепления материала решим несколько задач 

---
# Задача 1 

<p align="justify">Необходимо найти периметр треугольника со сторонами 3см, 4 см, 5 см. Ответ вывести в виде строки. </p>

```python
# Даны 3 переменные -> 3 4 5
a, b, c = map(int, input().split())

print(a + b + c) 
```

Ответ не верный. Попробуйте найти ошибку. 

```ad-custom-block
title: Показать правильный ответ
collapse: close
print(str(a + b + c))
```

---
# Задача 2

<p align="justify">Посчитайте площадь круга, радиусом 5 м. Число pi = 3,1415 Ответ предоставить в сантиметрах в целом виде</p>

Кто не помнит как считать площадь круга, смотрим в интернете.

```ad-custom-block
title: Показать правильный ответ
collapse: close
r = 5
pi = 3.1415
$s = int(pi * r ** 2)$
print(s*100)

или в 1 строку 
$print(int(5*3.1415**2)*100)$
```

---
# 📝 Домашнее задание 3
#home_work_lesson_3

- [ ] К следующему уроку иметь на счетчике +20 минут = 20 eng или 20 рус
- [ ] Знать как можно и как нельзя задавать переменные 
- [ ] Знать что такое PEP 8
- [ ] Литералы
- [ ] \n
- [ ] None
- [ ] Функция print() и ее именованные аргументы
- [ ] Функция input() 
- [ ] Арифметика над строками. Знать, что можно делать, а что нет.
- [ ] a, b = map(int, input().split())
- [ ] Написать мне если НЕ РАБОТАЕТ acmp.ru я скину задачи, которые нужно решить
- [ ] Задача 1 - [A+B](https://acmp.ru/index.asp?main=task&id_task=1)
- [ ] Задача 2 - [Игра](https://acmp.ru/index.asp?main=task&id_task=4)
- [ ] Задача 3 - [Два бандита](https://acmp.ru/index.asp?main=task&id_task=33)
- [ ] Задача 4 - [Бисер](https://acmp.ru/index.asp?main=task&id_task=903)
- [ ] Задача 5 - [Неглухой телефон](https://acmp.ru/index.asp?main=task&id_task=108)


[[#🧐 Более подробно о переменных в Python |Ввернуться на вверх ]]

